export type ResponseProps = {
  code: string
  name: string
}

export type CarProps = {
  vehicleType: number
  price: string
  brand: string
  model: string
  year: number
  fuelType: string
  code: string
  referenceMonth: string
  fuelInitial: string
}
